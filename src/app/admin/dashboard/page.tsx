"use client";
import {useQuery} from "@tanstack/react-query";import API from "@/api/api";import {Users,HardHat,CalendarDays,IndianRupee,AlertCircle,WalletCards,TrendingUp} from "lucide-react";
export default function Dashboard(){const{data,isLoading,error}=useQuery({queryKey:["admin-dashboard"],queryFn:async()=>(await API.get("/admin/dashboard")).data,retry:false});if(isLoading)return <div className="loading">Loading platform analytics…</div>;if(error||!data)return <div className="api-error"><h2>Could not load Admin analytics</h2><p>Make sure the API and MongoDB are running and you are logged in as Admin.</p></div>;const m=data.metrics,max=Math.max(...data.trend.map((x:any)=>x.bookings),1),cards=[["Customers",m.users,Users],["Total workers",m.workers,HardHat],["Total bookings",m.bookings,CalendarDays],["Monthly revenue",`₹${m.revenue.toLocaleString()}`,IndianRupee]] as const;return <div className="admin-page"><div className="page-heading"><div><small>LIVE OVERVIEW</small><h1>HomeServe+ operations</h1><p>All metrics below are aggregated from MongoDB.</p></div><span className="live-dot">● Live API</span></div><div className="metric-grid">{cards.map(([l,v,I])=><article key={l}><div><span>{l}</span><I/></div><strong>{v}</strong><small>Updated from current records</small></article>)}</div><div className="admin-panels"><section className="panel chart-panel"><header><div><h2>Booking trend</h2><p>Last 30 days</p></div></header>{data.trend.length?<div className="bar-chart">{data.trend.map((x:any)=><div key={x._id}><span style={{height:`${Math.max(x.bookings/max*100,8)}%`}} title={`${x.bookings} bookings`}/><small>{x._id.slice(-5)}</small></div>)}</div>:<div className="empty">No bookings in this period.</div>}</section><section className="panel"><header><div><h2>Needs attention</h2><p>Live pending totals</p></div></header><div className="attention"><a href="/admin/workers"><HardHat/><div><b>{m.pendingWorkers} worker verifications</b><small>Review documents</small></div></a><a href="/admin/complaints"><AlertCircle/><div><b>{m.pendingComplaints} complaints</b><small>Awaiting resolution</small></div></a><a href="/admin/payouts"><WalletCards/><div><b>{m.pendingPayouts} payouts</b><small>Pending processing</small></div></a><a href="/admin/bookings"><TrendingUp/><div><b>{m.pendingRefunds} refunds</b><small>Awaiting review</small></div></a></div></section></div><section className="panel status-panel"><header><div><h2>Booking statuses</h2><p>Live distribution by record count</p></div></header><div className="status-bars">{data.statuses.map((x:any)=><div key={x._id}><span>{x._id.replaceAll("_"," ")}</span><i><b style={{width:`${m.bookings?x.value/m.bookings*100:0}%`}}/></i><strong>{x.value}</strong></div>)}</div></section></div>}
