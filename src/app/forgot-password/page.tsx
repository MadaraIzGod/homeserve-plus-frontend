"use client";import {useState} from "react";import Link from "next/link";import API from "@/api/api";import {toast} from "sonner";export default function Forgot(){const[email,setEmail]=useState("");return <main className="auth-page"><Link href="/" className="brand">HomeServe<span>+</span></Link><form className="auth-card" onSubmit={async e=>{e.preventDefault();const{data}=await API.post("/auth/forgot-password",{email});toast.success(data.message)}}><small>ACCOUNT RECOVERY</small><h1>Reset your password.</h1><p>Enter your registered email address and we&apos;ll send reset instructions.</p><label><span>Email address</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button>Send reset link</button><div className="auth-switch"><Link href="/login">Back to login</Link></div></form></main>}
