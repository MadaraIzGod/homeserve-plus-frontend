"use client";import {useState} from "react";import API from "@/api/api";import {ImagePlus,Loader2,X} from "lucide-react";import {toast} from "sonner";
export async function uploadAsset(file:File){const signature=(await API.get("/uploads/signature")).data;if(signature.development)return await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=reject;r.readAsDataURL(file)});const form=new FormData();form.append("file",file);form.append("api_key",signature.apiKey);form.append("timestamp",String(signature.timestamp));form.append("signature",signature.signature);form.append("folder",signature.folder);const response=await fetch(`https://api.cloudinary.com/v1_1/${signature.cloudName}/auto/upload`,{method:"POST",body:form});if(!response.ok)throw new Error("Upload failed");return(await response.json()).secure_url as string}
export default function ImageUpload({value,onChange,label="Upload image"}:{value?:string;onChange:(url:string)=>void;label?:string}){const[busy,setBusy]=useState(false);return <div className="image-upload">{value?<div className="image-preview"><img src={value} alt="Uploaded preview"/><button type="button" onClick={()=>onChange("")}><X/></button></div>:<label className={busy?"busy":""}>{busy?<Loader2/>:<ImagePlus/>}<span>{busy?"Uploading…":label}</span><input type="file" accept="image/*" disabled={busy} onChange={async e=>{const f=e.target.files?.[0];if(!f)return;setBusy(true);try{onChange(await uploadAsset(f));toast.success("Image uploaded")}catch(err:any){toast.error(err.message)}finally{setBusy(false)}}}/></label>}</div>}
